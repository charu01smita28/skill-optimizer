"""Configuration data — pricing tables and detector calibration knobs.

Per-skill metadata (input glob, summary field, primary fields) lives in each
skill's SKILL.md frontmatter; see ``skill_optimizer.skill_md`` for the readers.
"""
